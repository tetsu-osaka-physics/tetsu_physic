\NeedsTeXFormat{pLaTeX2e}
\ProvidesPackage{h2b_rt_p}
\PackageInfo{h2b_rt_p}{expected global options: \MessageBreak
[11pt, a4paper, papersize, dvipdfmx, fleqn, bold]}

\usepackage{tetsu_physics}

\setlength{\oddsidemargin}{0truept}
\setlength{\topmargin}{4truept}

\setlength{\headheight}{0truept}
\setlength{\headsep}{0truept}

\setlength{\textheight}{700truept}
\setlength{\textwidth}{453truept}

\setlength{\marginparsep}{18truept}
\setlength{\marginparwidth}{18truept}

\setlength{\footskip}{28truept}
\setlength{\marginparpush}{16truept}

\setlength{\hoffset}{0truept}
\setlength{\voffset}{0truept}

\newcommand{\maru}[1]{\ajMaru{#1}}

\newcounter{dist}
\renewcommand{\thesection}{{\large 第\Combarabic{section}問}}
\def\@sect#1#2#3#4#5#6[#7]#8{%
  \ifnum #2>\c@secnumdepth
    \let\@svsec\@empty
  \else
    \refstepcounter{#1}%
    \protected@edef\@svsec{\@seccntformat{#1}\relax}%
  \fi
  \@tempskipa #5\relax
  \ifdim \@tempskipa<\z@
    \def\@svsechd{%
      #6{\hskip #3\relax
      \@svsec #8 }%
      \csname #1mark\endcsname{#7}%
      \addcontentsline{toc}{#1}{%
        \ifnum #2>\c@secnumdepth \else
          \protect\numberline{\csname the#1\endcsname}%
        \fi
        #7}}% 目次にフルネームを載せるなら #8
  \else
    \begingroup
      \interlinepenalty \@M % 下から移動
      #6{%
        \@hangfrom{\hskip #3\relax\@svsec}%
        \setcounter{dist}{#8}\hfill{\normalsize\thedist}% #8
         \@@par}%
    \endgroup
    \csname #1mark\endcsname{#7}%
    \addcontentsline{toc}{#1}{%
      \ifnum #2>\c@secnumdepth \else
        \protect\numberline{\csname the#1\endcsname}%
      \fi
      #7}% 目次にフルネームを載せるならここは #8
  \fi
  \@xsect{#5}}

\renewcommand{\thedist}{\textrm{（配点 \arabic{dist}点）}}

\renewcommand{\thefigure}{\Combarabic{figure}}
\renewcommand{\thetable}{\Combarabic{table}}

\captionsetup[figure]{font=normalsize}
% \captionsetup[table]{font=normalsize}

\@addtoreset{equation}{section}
\@addtoreset{figure}{section}
\@addtoreset{table}{section}

\pagestyle{empty}
